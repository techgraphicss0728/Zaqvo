name: zaqvo_delivery_app
description: Zaqvo delivery partner app (Android & iOS).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  dio: ^5.7.0
  go_router: ^14.6.2
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
